buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.banderous.getpack:plugin:1.+'
    }
}

subprojects {
    apply plugin: 'com.banderous.getpack'
}

